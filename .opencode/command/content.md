---
description: Apply a content/copy change to the CV site (config.json only), rebuild and verify. Usage - /content <describe the change>
agent: content-editor
subtask: true
---

Apply this content change to config.json, rebuild the site (and PDF if profile,
experience or skills content changed), run the autotest suite and report the diff + result:

$ARGUMENTS
