---
description: Regenerate the CV PDF export (or a custom export) via the pdf-publisher agent. Usage - /pdf [extra flags or instructions]
agent: pdf-publisher
subtask: true
---

Generate the CV PDF export from config.json, verify it is a single A4 page and
visually inspect the result as defined in your instructions. Report file, size, page count
and visual verdict. $ARGUMENTS
